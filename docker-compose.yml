services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: neurelix
      POSTGRES_USER: neurelix
      POSTGRES_PASSWORD: neurelix
    ports:
      - "5432:5432"
    volumes:
      - neurelix_pg:/var/lib/postgresql/data
volumes:
  neurelix_pg:
